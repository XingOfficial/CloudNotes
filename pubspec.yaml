name: cloud_notes
description: 云笔记 - 简洁高效的云端笔记应用
publish_to: 'none'
version: 1.0.3+4

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.2
  intl: ^0.20.3
  share_plus: ^7.2.1
  url_launcher: ^6.2.1
  flutter_local_notifications: ^17.2.2
  timezone: ^0.9.4
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
